export default function FinishWorkout({exerciseName, rows, sets})
{
    console.log("Exercise Name:", exerciseName);
        console.log("Rows:", rows);
        console.log("Sets:", sets);

    return(<div>

            {/* For finWorkout I get name from Workout */}
            {/* Rows from workoutinput */}
            {/* And all the other info from set, i think */}


    </div>);
}